We use libritts as the corpus and subject id QZRlT5NqTgs34Uz6r1me (Elio) from ElevanLabs to generate the synthetic data. The synthesized result is stored under ./raw_data

After generating the synthesized data, run preprocess.ipynb to create the metadata file (./metadata/speakers.csv)

TODO:
We use seed vc to generate the spanish accented version of LibriTTS speech.